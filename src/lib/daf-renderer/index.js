// Re-export the main renderer
export { default } from './renderer.js';
export { defaultOptions } from './options.js';

// Types are available via TypeScript declaration files
// Import them in TypeScript files with: import type { DafRenderer } from '$lib/daf-renderer/types';